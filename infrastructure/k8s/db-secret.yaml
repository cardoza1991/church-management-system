apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: church-mgmt
type: Opaque
data:
  # These are base64 encoded - in production, use actual secure values
  DB_USER: YWRtaW4=  # "admin" in base64
  DB_PASSWORD: cGFzc3dvcmQ=  # "password" in base64
